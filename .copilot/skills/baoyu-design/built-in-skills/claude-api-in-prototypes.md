---
name: "claude-api-in-prototypes"
description: "Claude API in prototypes\nCall Claude from your HTML artifacts via window.claude.complete"
---
HTML artifacts can call Claude via window.claude.complete without an SDK or
API key. Use claude-haiku-4-5 with the built-in output cap and per-user rate
limit; call it with either a string prompt or a messages array.
